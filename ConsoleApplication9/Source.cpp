#include<iostream>
#include"Reservoir.h"
#include<time.h>
using namespace std;
void main()
{
	srand(time(o));
	int n = 5;
	Reservoir* a = new Reservoir[n];
	Reservoir w;
	int = a[2];
	int p = 0;
	for (int i = 0; i < n; i++)
	{
		a[i].Volume();
		a[i].Area();
		a[i].Srav();
		if (a[i].Getsea() > 12000)
		{
			p++;
		}
	}
	cout << p << endl;
	a[2].Show();
	a[2].Add("sasas");
	a[2].Show();
     
	b = a[2];
	b.Show();
	
	cout << endl;
	a.Volume();
	a.Area();
	a.Srav();
	delet[] a;
}